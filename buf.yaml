version: v2

modules:
  - name: buf.build/antimetal/apis
    path: api

breaking:
  use:
    - FILE
lint:
  use:
    - STANDARD
  except:
    - ENUM_ZERO_VALUE_SUFFIX

deps:
  - buf.build/googleapis/googleapis

